<%- include('partials/header.ejs'); %>
    <body>
        <%- include('partials/navbar.ejs'); %>
        <div class="container">
            <hr>
            <div class="header">
                <h1>Perguntas</h1>
                <a href="/ask" class="btn btn-primary">Perguntar</a>
            </div>
            <hr>
            <% if(asks.length <= 0) { %>
                <div class="card">
                    <div class="card-header">
                        <h3>Sem perguntas :(</h3>
                    </div>
                    <div class="card-body">
                        <p>Caso tenha alguma dúvida, pode realizar a sua pergunta clicando no botão azul ao lado.</p>
                    </div>
                </div>
            <%}%>
            <% asks.forEach(ask => { %>
                <br>
                <div class="card">
                    <div class="card-body">
                        <h3><%= ask.title %></h3>
                    </div>
                    <div class="card-footer">
                        <a href="/reply/<%= ask.id %>"><button class="btn btn-primary">Responder</button></a>
                    </div>
                </div>
            <% }) %>

        </div>
        <!-- JS -->
        <script src="/public/script/script.js"></script>
    </body>

<%- include('partials/footer.ejs'); %>